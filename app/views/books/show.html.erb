<div class="book-body">
  <p></p>
  <p><%= @book.user.name %></p>
  <p><%= @book.title %></p>
  <p><%= @book.body %></p>
  <%if @book.user == current_user %>
    <%= button_to "Destroy", book_path(@book), method: :delete %>
  <% end %>
</div>
